import { getSuppliers, getItems } from '../function/getData';

const testSuppliersList = ['A', 'B', 'C', 'D']; // null - заглушка
const testItemsList = ['a', 'b', 'c'];

export { testSuppliersList, testItemsList };
